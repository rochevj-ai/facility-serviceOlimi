rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For MVP — open read/write. Tighten with Firebase Auth later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
